# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
pgvector==0.3.2
alembic==1.13.3

# Settings / validation
pydantic==2.9.2
pydantic-settings==2.5.2

# HTTP client (for Ollama, NCBI, CDC, WHO, FDA APIs)
httpx==0.27.2

# Embeddings (all-MiniLM-L6-v2, 22 MB)
sentence-transformers==3.1.1

# Scheduling (background ingestion jobs)
apscheduler==3.10.4

# Monitoring
prometheus-client==0.21.0

# Docker SDK – lets the backend start/stop the Ollama container on demand
docker==7.1.0

# Utilities
python-multipart==0.0.12   # for form data (future use)
